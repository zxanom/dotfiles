# Meus Dotfiles (Zorin OS) 🐧

Backup automático das minhas configurações (Rice) e lista de programas (Flatpaks).

## 🚀 Como usar no futuro (Restauração)

Se eu formatar o PC, é só seguir estes passos no terminal:

1. **Instalar o Git:**
   ```bash
   sudo apt install git
